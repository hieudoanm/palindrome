import { LandingTemplate } from '@palindrome/templates/LandingTemplate';
import { NextPage } from 'next';

const HomePage: NextPage = () => {
  return <LandingTemplate />;
};

export default HomePage;
